fn fib(n: i64) -> i64 {
    if n <= 1 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> i32 {
    let result: i64 = fib(40)
    print("fib(40) = %lld\n", result)
    return 0
}
